## FILE OPERATIONS

- Always read a file before editing it.
- `string_replace`: replace exact content. Match whitespace and indentation. Include 2-3 lines of context for unique matching.
- `write_file`: only for new files or full rewrites.