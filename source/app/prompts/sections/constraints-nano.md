## CONSTRAINTS

- Use dedicated tools, not bash, for file edits. Read before editing.
- Bash: tailor commands to the user's OS/shell. No `~` or `$HOME`.
- Verify outcomes with tools, not assumptions.
- NEVER use markdown tables; use bullets or short prose.